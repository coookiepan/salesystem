# DUSKIN 銷售系統 — 完整設定指南

> 這份指南會帶你從零開始，一步一步把 DUSKIN 銷售系統建置起來。  
> 不需要任何技術背景，只要照著做就好！

---

## 📋 目錄

1. [這是什麼？](#這是什麼)
2. [你需要準備的東西](#你需要準備的東西)
3. [Step 1：Fork GitHub 專案](#step-1fork-github-專案)
4. [Step 2：建立 Google 試算表](#step-2建立-google-試算表)
5. [Step 3：設定 Google Apps Script](#step-3設定-google-apps-script)
6. [Step 4：部署 Apps Script 為網頁應用程式](#step-4部署-apps-script-為網頁應用程式)
7. [Step 5：連接 App 與 Google Sheets](#step-5連接-app-與-google-sheets)
8. [Step 6：（選用）店家查詢資料](#step-6選用店家查詢資料)
9. [功能總覽](#功能總覽)
10. [重要注意事項](#重要注意事項)
11. [常見問題排解](#常見問題排解)
12. [長官儀表板（多業務員彙總用）](#長官儀表板多業務員彙總用)
13. [工業區CRM（陌生開發用）](#工業區crm陌生開發用)
14. [工地地圖（施工店面陌生開發）](#工地地圖施工店面陌生開發)
15. [版本更新紀錄](#-版本更新紀錄)（內容在 [CHANGELOG.md](CHANGELOG.md)）

---

## 這是什麼？

DUSKIN 銷售系統是一個專為 **外勤業務人員** 設計的行動網頁應用程式（PWA）。

它可以幫你：

- **管理客戶資料** — 記錄客戶名稱、地址、聯絡人、狀態（未拜訪、試用中、已成約...）
- **記錄拜訪日誌** — 每次拜訪的筆記、心情、照片
- **追蹤試用品項** — 哪些客戶正在試用什麼商品、報價多少
- **管理車上庫存** — 即時掌握拖把、地墊、芳香劑的庫存數量
- **產出日報** — 自動彙整每天的拜訪紀錄
- **待辦清單** — 所有客戶的待辦事項一覽
- **店家查詢** — 搜尋公司現有客戶名單

### 特色

- 📱 **手機優先設計** — 在手機上操作像原生 App 一樣順暢
- 🔌 **離線可用** — 沒有網路也能正常使用，資料存在你的手機裡
- ☁️ **雲端備份** — 連接 Google Sheets 後，資料會同步到雲端，不怕手機資料遺失
- 🆓 **完全免費** — 使用 GitHub Pages 免費部署，不需要付任何費用

---

## 你需要準備的東西

在開始之前，請確認你有以下這些：

| 項目 | 說明 |
|------|------|
| **Google 帳號** | 用來建立 Google Sheets 儲存資料（如果你有 Gmail，就已經有了） |
| **GitHub 帳號** | 用來放置並部署這個網頁應用程式（免費註冊） |
| **電腦** | 設定過程建議用電腦操作，設定完成後就可以用手機 |

如果你還沒有 GitHub 帳號，請先到 [github.com](https://github.com) 註冊一個。過程很簡單，只需要 email 和密碼。

---

## Step 1：Fork GitHub 專案

> 「Fork」就是把別人的專案複製一份到你自己的帳號底下。這樣你就有了一個屬於自己的版本。

### 1.1 打開原始專案

用電腦瀏覽器打開這個網址：

👉 **https://github.com/coookiepan/salesystem**

### 1.2 點擊 Fork

你會看到頁面右上角有一個 **「Fork」** 按鈕（旁邊通常有一個數字）。

1. 點擊 **Fork**
2. 跳出的頁面中，「Owner」（擁有者）選你自己的帳號
3. 「Repository name」保持預設的 `salesystem` 就好
4. 點擊綠色的 **「Create fork」** 按鈕

等幾秒鐘，你會看到頁面跳轉到 **你自己帳號下** 的 `salesystem` 專案。網址會變成 `https://github.com/你的帳號名稱/salesystem`。

### 1.3 開啟 GitHub Pages

現在要讓這個專案變成一個可以打開的網頁：

1. 在你 Fork 後的專案頁面，點上方的 **「Settings」**（設定，齒輪圖示）
2. 左邊選單往下找到 **「Pages」**，點擊它
3. 你會看到「Build and deployment」區塊
4. 在 **「Source」** 那一行，點下拉選單，選擇 **「Deploy from a branch」**
5. 在 **「Branch」** 那一行，把 `None` 改成 **`main`**，旁邊的資料夾選 **`/ (root)`**
6. 點擊 **「Save」**

### 1.4 等待部署完成

GitHub 會花大約 1~3 分鐘來部署你的網站。部署完成後：

- 回到 **Settings → Pages** 頁面
- 你會在上方看到一個綠色方框，寫著：「Your site is live at...」
- 你的網站網址就是：

```
https://你的帳號名稱.github.io/salesystem/
```

用手機瀏覽器打開這個網址，你會看到 DUSKIN 銷售系統的畫面！🎉

> 💡 **小提示**：第一次可能需要等幾分鐘才能打開。如果顯示 404，請耐心等待，重新整理頁面試試。

---

## Step 2：建立 Google 試算表

> Google 試算表會作為你的「雲端資料庫」，所有客戶資料都會備份到這裡。

### 2.1 打開 Google Sheets

用瀏覽器打開：

👉 **https://sheets.google.com**

如果還沒登入 Google，系統會請你登入。

### 2.2 建立空白試算表

1. 點擊左上角的 **「+」**（空白試算表），或點擊「空白」的方格
2. 你會看到一個新的空白試算表被建立出來

### 2.3 幫試算表取個名字

1. 左上角會顯示「未命名的試算表」
2. 點擊它，改成一個好辨識的名字
3. 按 Enter 確認

> ⚠ **重要：命名規則（多人團隊必看）**
>
> 如果你的公司會有多位業務員共用這套系統，**請把試算表檔名直接設為你的姓名**（例如：「王大明」、「陳小華」）。
>
> 原因：長官端的儀表板會自動讀取每個業務員試算表的「整份檔名」當作業務員身份，並把該業務員的試用金額、成約金額等指標顯示出來。檔名取錯，儀表板上會顯示錯誤的業務員名稱。
>
> ✅ 建議：`王大明`
> ✅ 建議：`陳小華 - 銷售系統`（檔名前段是姓名即可）
> ❌ 避免：`銷售系統`、`未命名的試算表`、`Untitled spreadsheet`

你不需要在試算表裡手動建立任何工作表（sheet）。稍後 App 連線成功並「全部推送」後，系統會自動幫你建立需要的工作表，包括：`clients`（客戶）、`inventory`（庫存）、`products`（商品）、`shops`（店家）。

> 💡 **記住**：先不要關掉這個試算表頁面，接下來的步驟需要用到它。

---

## Step 3：設定 Google Apps Script

> Apps Script 是 Google 提供的程式碼執行平台。我們需要在試算表裡放一段程式碼，讓 App 能跟試算表溝通。你不需要懂程式碼，只要複製貼上就好。

### 3.1 打開 Apps Script 編輯器

1. 在你剛才建立的 Google 試算表中
2. 點上方選單的 **「擴充功能」**（Extensions）
3. 在下拉選單中點 **「Apps Script」**

你會看到一個新的分頁被打開，這就是 Apps Script 的程式碼編輯器。畫面上會有一些預設的程式碼（通常是 `function myFunction() { }` 之類的）。

### 3.2 從 App 複製程式碼

1. 用瀏覽器打開你的 DUSKIN 銷售系統（就是 Step 1 建好的那個網址）
2. 點底部選單的 **「設定」**（齒輪圖示，最右邊的按鈕）
3. 往下滾動，你會看到「設定步驟」區塊
4. 找到最下方的按鈕：**「複製 Apps Script 程式碼」**
5. 點擊它，系統會顯示 **「✅ 已複製 Apps Script 程式碼」**

程式碼已經複製到你的剪貼簿了！

### 3.3 貼入 Apps Script 編輯器

1. 切回到 Apps Script 編輯器的分頁
2. **全選**編輯器裡現有的所有程式碼（按 `Ctrl+A`，Mac 使用者按 `Cmd+A`）
3. **刪除**它們（按 `Delete` 或 `Backspace`）
4. **貼上**剛才複製的程式碼（按 `Ctrl+V`，Mac 使用者按 `Cmd+V`）

你會看到編輯器裡出現一大段新的程式碼。

### 3.4 儲存

按 `Ctrl+S`（Mac 使用者按 `Cmd+S`），或點選上方工具列的 💾 儲存圖示。

你會看到上方顯示「已儲存專案」的提示。

---

## Step 4：部署 Apps Script 為網頁應用程式

> 「部署」就是把程式碼發佈到網路上，讓你的 App 能透過一個網址來存取它。

### 4.1 開始部署

1. 在 Apps Script 編輯器中，點右上角的藍色 **「部署」** 按鈕（Deploy）
2. 選擇 **「新增部署作業」**（New deployment）

### 4.2 設定部署選項

你會看到一個彈出視窗：

1. 左上角「選取類型」旁邊有一個 ⚙️ 齒輪圖示，點擊它
2. 選擇 **「網頁應用程式」**（Web app）

接著填寫以下設定：

| 欄位 | 填什麼 |
|------|--------|
| **說明**（Description） | 隨便填，例如 `v1` |
| **執行身分**（Execute as） | 選擇 **「我」**（Me） |
| **誰可以存取**（Who has access） | 選擇 **「任何人」**（Anyone） |

3. 點擊 **「部署」**（Deploy）

### 4.3 授權（重要！第一次才需要）

因為這段程式碼需要存取你的 Google 試算表，Google 會請你授權：

1. 點 **「授權存取」**（Authorize access）
2. 選擇你的 **Google 帳號**
3. 你可能會看到一個警告畫面，寫著「Google hasn't verified this app」（Google 尚未驗證此應用程式）—— 這是正常的！因為這是你自己寫的（貼的）程式碼。
4. 點左下角的 **「Advanced」**（進階）
5. 點 **「Go to [你的專案名稱] (unsafe)」**（前往...（不安全））
6. 點 **「Allow」**（允許）

> ⚠️ 這個步驟很多人會卡住。看到「不安全」不用緊張，這只是因為 Google 沒有審核過你自己的程式碼而已，完全安全。

### 4.4 複製部署網址

授權完成後，你會看到一個「部署完成」的畫面，上面有一段 **Web App URL**，長得像這樣：

```
https://script.google.com/macros/s/AKfycbx..........很長一串.........../exec
```

**這段網址非常重要！** 請點旁邊的 **「複製」** 按鈕把它複製下來。

> 💡 建議把這段網址先貼到記事本或 LINE 的自己的聊天室，以免搞丟。

---

## Step 5：連接 App 與 Google Sheets

> 現在回到你的 DUSKIN 銷售系統 App，把 Google Sheets 跟 App 連起來。

### 5.1 貼上網址

1. 打開你的 DUSKIN 銷售系統（`https://你的帳號名稱.github.io/salesystem/`）
2. 點底部選單的 **「設定」**（最右邊的齒輪圖示）
3. 找到 **「Google Sheets 同步」** 區塊
4. 在 **「Apps Script Web App URL」** 的輸入框裡，貼上你剛才複製的那段 URL
5. 系統會自動儲存這個網址

### 5.2 測試連線

1. 點 **「測試連線」** 按鈕
2. 等幾秒鐘，你會看到上方出現一個綠色提示框，顯示連線成功的訊息

如果連線成功，代表 App 已經能跟你的 Google 試算表溝通了！

如果顯示錯誤，請參考下方的[常見問題排解](#常見問題排解)。

### 5.3 推送資料到雲端

1. 點 **「全部推送」** 按鈕
2. 確認對話框點「確定」
3. 等待推送完成

完成後，你可以切回 Google 試算表看看，會發現自動新增了好幾個工作表（`clients`、`inventory`、`products` 等），裡面就是你的資料！

🎊 **恭喜！設定完成！** 你現在可以開始使用 DUSKIN 銷售系統了。

---

## Step 6：（選用）店家查詢資料

> 「查詢」功能可以讓你搜尋公司現有的客戶名單。如果你手上有這份名單，可以把它放進 Google 試算表裡。

### 6.1 找到 shops 工作表

1. 打開你的 Google 試算表
2. 在完成「全部推送」之後，你會在底部看到一個叫做 **`shops`** 的工作表標籤
3. 點擊它切換過去

### 6.2 填入店家資料

`shops` 工作表的欄位如下：

| 欄位 A | 欄位 B | 欄位 C | 欄位 D |
|--------|--------|--------|--------|
| 顧客名 | 電話號碼 | 地址 | 負責人 |

你可以手動逐筆輸入，也可以用複製貼上的方式把已有的名單貼進去。

> 💡 **小提示**：如果你手上有 Excel 格式的名單，可以直接把對應的欄位複製貼上到 Google Sheets 裡。

### 6.3 在 App 中載入

1. 回到 DUSKIN 銷售系統 App
2. 點底部選單的 **「查詢」**（放大鏡圖示）
3. 點右上角的 **「🔄 重新載入」** 按鈕
4. 等待載入完成，你就可以用搜尋框搜尋店名、地址、電話、負責人了

---

## 功能總覽

以下簡單介紹每個功能頁面的用途：

### 🏠 首頁（從桌面圖示打開的第一個畫面）

- **今日行程**：今天要跑的前三站（含區域路線）、逾期紅色提醒，「開始跑 →」直達行程規劃。
- **四個數字**：今日行程／逾期／試用該收／今日待辦，點了直達對應清單。
- **快速動作**：登記工地（直接 GPS 放點）、新客戶（直接開表單）、找客戶、寫日報。
- **本週成果**：拜訪／成約／新試用自動統計，一鍵進日報表與 4W 推進表。
- 三個系統（客戶管理／工地地圖／工業區CRM）底部都有「🏠 首頁」，一鍵回家。
- 首頁數字直接讀手機裡的資料，**離線也看得到**；右上角顯示三個系統的同步狀態。

### 👥 客戶（底部選單第 1 個）

這是你最常用的頁面。

- **客戶列表**：所有客戶一覽，可用狀態分類篩選（未拜訪、初訪、複訪、試用中、報價、將成約、已成約、已轉交、拒絕）
- **搜尋**：輸入關鍵字即時搜尋客戶
- **排序**：依試用日、下次拜訪日、狀態、最後更新、名稱排列
- **新增客戶**：點「+ 新增」按鈕，填入基本資料（名稱、地址、類型、狀態...）
- **客戶詳情**：點擊任一客戶可查看完整資訊、拜訪記錄、試用品項
- **記錄拜訪**：在客戶詳情頁面新增拜訪紀錄（包含日期、筆記、心情）
- **試用品項**：追蹤客戶正在試用哪些商品、報價金額
- **📄 產生合約**：客戶詳情頁右上角按鈕，一鍵帶入該客戶資料產生 Word 報價單／合約（詳見下方「產生合約」段落）

### 📅 日報（底部選單第 2 個）

- 選擇日期，查看當天所有的拜訪記錄
- 自動彙整統計數據（拜訪數、新客戶數等）
- 方便回顧和製作日報

### ✅ 待辦（底部選單第 3 個）

- 集中顯示所有客戶的待辦事項
- 勾選已完成的項目
- 可篩選未完成 / 已完成
- 上方可切到「**🗓 今日行程**」：
  - 行程＝「下次拜訪日為今天」的客戶，會依**區→路**排好，三台裝置自動同步
  - **逾期不會蓋住今天**：收在下方紅色區，可逐一「今天/改期/清除」或一鍵「全部移到今天」
  - **💡 順路建議**：依你今天要跑的區，自動列出同區＋順路（例：去官田，新市/善化是「途中」、六甲是「再過去一站」）的店家——有待辦的、未拜訪的、成交可順路關懷的，按「＋拉進今天」即可

### 🔍 查詢（底部選單第 4 個）

- 搜尋公司現有客戶名單（資料來自 Google Sheets 的 `shops` 工作表）
- 輸入店名、地址、電話、負責人都能搜尋到
- 點「重新載入」可從雲端取得最新資料

### 📦 庫存（底部選單第 5 個）

- 管理你車上的庫存數量
- 分類瀏覽：地墊、拖把、芳香
- **+1 / -1**：快速調整庫存數量
- **缺貨清單**：一鍵查看哪些品項需要補貨
- 搜尋品項代號

### ⚙️ 設定（底部選單第 6 個）

- **商品資料庫**：管理所有商品（代號、品名、單價、計費週期）
- **業務個人資料（我的名片）**：填寫你自己的姓名、手機、電話、傳真、Email、營業所名稱與地址；這些會自動帶入報價單與合約的「承辦人（乙方）」欄位（詳見下方）
- **版本資訊**：查看目前使用的版本號，點「強制重新載入」更新到最新版
- **Google Sheets 同步**：設定連線、測試連線、推送 / 拉取資料
- **資料管理**：匯出 JSON 備份 / 從 JSON 備份匯入

### 📇 設定你的業務個人資料（第一次使用先做）

報價單與合約上的「承辦人、手機、電話、Email、地址、營業所名稱」都來自這份個人資料，**填一次就好**，之後每次產出文件都會自動帶入你自己的資訊。

**怎麼設定：**

1. 底部選單點 **⚙️ 設定** → 找到 **業務個人資料（我的名片）**
2. 填入你的姓名、手機、電話、傳真、Email、營業所名稱與地址（邊打邊存，不必按儲存鈕）
3. （選填）填 **業務編號**、**職稱**，並可上傳 **電子簽名／印章圖檔**——印章會印在合約書的「乙方（出租人）」欄位
4. 完成後做報價單／合約時就會自動帶入

**注意：**

- 這份資料**只存在你自己的手機**，不會上傳雲端、也不會被別人看到；它會一起包含在「匯出 JSON 備份」裡。
- 若還沒填，第一次做報價單／合約時會跳一次提醒，問你要不要先去填；選「就用預設繼續」之後就不再提醒。
- 沒填時會沿用系統預設（潘秉均／台南營業所）。

> 💡 設定頁最上方有「**上手檢查清單**」，會用綠勾／待辦提醒你：個人資料、雲端連線、試算表命名三件事是否都完成。

### 🏢 公司導入多位業務

本系統設計為「**每位業務各自獨立**」：大家開**同一個網址**，但各自設定**自己的 Google 試算表**與**個人資料**，資料彼此獨立、互不可見。導入時請注意：

1. **共用同一個網址**：所有業務都用同一份 GitHub Pages 網址，**不要各自 Fork**。這樣只要維護者更新一次，全體就會拿到新版；各自 Fork 會造成版本分裂、更新提示也各看各的。
2. **每位業務各建自己的試算表**：照本指南 Step 2～5 各自建立試算表、部署 Apps Script、貼上自己的 URL。
3. **試算表用「業務姓名」當檔名**：主管儀表板靠**試算表的整份檔名**辨識業務員（例如把試算表命名為「王大明」）。請確保檔名與個人資料的姓名一致——設定頁的「上手檢查清單」會自動幫你比對並提醒。
4. **填寫個人資料**：每位業務都要填自己的「業務個人資料」，報價單／合約才會帶出正確的承辦人。

**主管彙總（唯讀）**：主管在網址後加 `?admin=1` 開啟，於「版本選單 → 📊 長官儀表板」貼上各業務的 Apps Script URL（一行一個），即可彙總所有人的試用／成約軒數與金額。

**離職／交接**：每位業務的資料存在**他自己的 Google 帳號試算表**裡。當業務離職或交接時，請務必先處理其中一項，否則公司會失去那份資料：

- **轉移試算表擁有權**：在 Google Sheets 將該試算表「共用 → 擁有權轉移」給接手者或主管帳號；接手者把新網址重新部署後貼回 App，或
- **匯出備份**：離職者在「設定 → 資料管理 → 匯出 JSON 備份」匯出檔案交接保存。

### 📄 產生合約／報價單

整合自 [contractmaker](https://github.com/coookiepan/contractmaker) 的 Word 報價單／合約產生器，業務員不需離開銷售系統，從客戶詳情頁就能一鍵產出 .docx 檔。

**怎麼用：**

1. 進入任一客戶詳情頁（在「客戶」分頁點擊客戶卡片）
2. 右上角點 **📄 產生合約** 按鈕
3. 系統會跳出編輯器，並自動帶入：
   - **客戶名稱** ← `name`
   - **聯絡人** ← `contact`
   - **客戶地址** ← `addr`
   - **日期** ← 今天
   - **商品明細** ← 客戶的成約品項（若無則用試用品項）
     - 自動換算：`quoted` 欄位（4W 總額）→ 報價單上的「特殊單價」（4W 每件）
     - 自動排除：bundled 配件（如把手、桿子）
     - 自動略過：合約商品庫沒有的代號（會在頂部訊息列提示）
4. 確認／微調欄位：
   - 統一編號、客戶電話：客戶資料若有可手動補
   - 顯示週期：預設 4W（月單價）
   - 整體折扣：填 0~1 之間（例：0.8 = 8 折）
   - 額外備註：一行一條
5. 需要時可手動編輯：
   - **代號**：直接覆寫；若代號在商品庫內，會自動帶入品名／單價；不在商品庫的代號會變「臨時新品」
   - **品名**：可改成客製名稱（會以 `nameOverride` 出現在 Word 上，不影響計價）
   - **特殊單價**：你跟客戶議好的價格
   - **臨時新品**：商品庫沒有的代號用「🆕 臨時新品」按鈕新增，需填代號 + 品名 + 單價
6. 點 **📋 預覽** 查看文字版內容
7. 點 **📥 下載 Word** 產出 .docx
8. 下載成功後，系統會自動在該客戶的「拜訪記錄」加一筆「已產生報價單：XXX.docx」紀錄，方便日後追溯

**注意事項：**

- 第一次點開時需從網路下載 docx 函式庫（~600KB），請保持網路連線
- 合約商品庫（120 項）獨立於銷售系統商品庫；若客戶試用／成約品項的代號不在合約商品庫，會自動建為「臨時新品」並從銷售系統商品庫補品名／單價（無單價時請手動補）
- 產出檔名格式：`報價單_{客戶名稱}{_版本後綴}.docx`
- 產出的 Word 含公司資訊、客戶資料、商品明細表、說明、核章區（可在「全域設定」勾選關閉）

**📱 手機操作：**

- **Android Chrome / 桌面 Chrome / Edge**：點「📥 下載 Word」會直接下載到「下載」資料夾
- **iPhone / iPad Safari**：因為 iOS 不支援直接觸發下載，點下載後會跳出 **系統分享面板**，請選：
  - **「儲存到檔案」** → 存到 iCloud Drive 或本機檔案
  - 或 **Mail / 訊息 / LINE** → 直接傳給客戶或自己
  - 若沒跳分享面板（舊版 iOS），會在新分頁開啟下載連結，再點該頁的下載按鈕即可
- 模態框會自動填滿手機螢幕，可上下滾動編輯所有欄位

---

## 重要注意事項

### 🔄 Apps Script 程式碼更新

當 DUSKIN 銷售系統更新版本時，有時候 Apps Script 的程式碼也會跟著更新。如果你看到 App 提示需要更新後端程式碼，請依照以下步驟操作：

1. 在 App 的「設定」頁面，點 **「複製 Apps Script 程式碼」** 取得最新程式碼
2. 打開 Google Apps Script 編輯器（Google 試算表 → 擴充功能 → Apps Script）
3. 全選舊程式碼 → 刪除 → 貼上新程式碼 → 儲存
4. 點 **「部署」** → **「管理部署作業」**（Manage deployments）
5. 點右邊的 ✏️ 編輯圖示
6. 在「版本」（Version）那裡，把它改成 **「新版本」**（New version）
7. 點 **「部署」**

> ⚠️ 這邊容易出錯的地方：一定要選「新版本」，不要選舊的版本號。否則新程式碼不會生效。

### 💾 資料儲存方式

- App 的資料主要存在瀏覽器的 **localStorage** 裡（就是你手機/電腦瀏覽器的本地儲存空間）
- 如果你清除瀏覽器資料、清除快取、或換一台手機，本機資料就會消失
- 所以一定要設定好 Google Sheets 連線，定期「全部推送」來備份資料
- 萬一本機資料遺失，可以用「從雲端拉取」把資料從 Google Sheets 拉回來

### 📴 離線使用

- App 在沒有網路的狀況下也能正常運作
- 你可以繼續新增客戶、記錄拜訪、調整庫存
- 等有網路時，再去「設定」裡按「全部推送」同步到雲端

### 📱 加到手機桌面

讓 App 用起來像原生應用程式一樣方便：

**iPhone（Safari）：**
1. 用 Safari 打開你的 App 網址
2. 點底部的「分享」按鈕（方框+向上箭頭的圖示）
3. 往下找到 **「加入主畫面」**
4. 點「新增」

**Android（Chrome）：**
1. 用 Chrome 打開你的 App 網址
2. 點右上角的「三個點」選單
3. 選擇 **「加到主畫面」** 或 **「安裝應用程式」**
4. 點「新增」

加到桌面後，你會在桌面上看到一個 App 圖示，點開就直接進入銷售系統，跟一般 App 一樣。

### 📲 多裝置使用

如果你想在手機和電腦上都使用，或者換了新手機：

1. 在每一台裝置上，都打開同一個 App 網址
2. 在「設定」裡都貼上 **同一個** Google Sheets Web App URL
3. 在新裝置上，點 **「從雲端拉取」** 就可以把所有資料同步過來

> 💡 注意：如果兩台裝置都有修改資料，記得先在一台推送，再到另一台拉取，避免資料衝突。

---

## 常見問題排解

### ❌ 「測試連線失敗」

可能原因與解決方法：

1. **URL 貼錯了**  
   → 確認你貼的是完整的 Web App URL，開頭是 `https://script.google.com/macros/s/` 結尾是 `/exec`

2. **Apps Script 還沒部署**  
   → 回到 Apps Script 編輯器，確認已經執行了「部署 → 新增部署作業」的步驟

3. **沒有授權**  
   → 第一次部署時需要授權。如果跳過了，請重新部署一次

4. **權限設定不對**  
   → 確認部署時「誰可以存取」設定為 **「任何人」**

### ❌ 「版本過舊」

- 打開 App 的「設定」頁面
- 點 **「🔄 強制重新載入」** 按鈕
- 這會清除瀏覽器快取並重新載入最新版本

### ❌ 「資料消失了」

如果你發現客戶資料不見了，可能是瀏覽器的 localStorage 被清除了（例如清除瀏覽紀錄時一併清除了）。

**解決方法：**
1. 打開 App →「設定」
2. 確認 Apps Script Web App URL 還在（如果也被清掉，重新貼上）
3. 點 **「從雲端拉取」**
4. 系統會從 Google Sheets 下載所有資料回來

> 前提是你之前有成功「全部推送」過。如果從來沒推送過，Google Sheets 裡就沒有資料可以拉取。

### ❌ Apps Script 顯示「Script function not found」

- 回到 Apps Script 編輯器
- 確認你已經貼上了 **完整的** 程式碼
- 確認沒有只複製到一半
- 確認已按 `Ctrl+S` 儲存
- 重新部署：部署 → 管理部署作業 → 編輯 → 版本選「新版本」→ 部署

### ❌ 打開網頁顯示 404 Not Found

- 確認你已經在 GitHub 的 Settings → Pages 裡啟用了 GitHub Pages
- 確認 Branch 選的是 `main`
- 部署需要 1~3 分鐘，請稍等後重新整理頁面
- 確認網址格式正確：`https://你的帳號名稱.github.io/salesystem/`

### ❌ Google 授權畫面顯示「This app is blocked」

這通常發生在使用公司的 Google Workspace 帳號時。解決方法：
- 改用個人的 Gmail 帳號來建立試算表和 Apps Script
- 或聯繫你的公司 IT 管理員開放權限

---

## 長官儀表板（多業務員彙總用）

如果公司有多位業務員，長官（主管）想看所有業務員的銷售數字，使用這個功能。

### 啟用方式

長官在原本的 CRM 網址後面加上 `?admin=1`：

```
https://你的帳號名稱.github.io/salesystem/?admin=1
```

建議**把這個網址加進書籤**。一般業務員的網址不要加 `?admin=1`，他們不會看到長官按鈕。

### 設定步驟

1. 用 `?admin=1` 網址開啟 CRM
2. 右上角點版本號 → 出現黃色按鈕「📊 長官儀表板」
3. 在彈出的視窗中：
   - 把每位業務員的 Apps Script URL **一行一個**貼進文字框
   - 按「儲存清單」
   - 按「載入彙總」
4. **概覽分頁**會自動顯示每位業務員的：
   - **試用軒數**（狀態為試用中／報價／將成約的店家數）
   - **試用金額**（試用品項的 4W 原價加總，公司預設 2W 換週）
   - **成約軒數**（狀態為已成約的店家數）
   - **成約金額**（成約品項的 4W 報價加總，若無報價則用原價）
   - **成約轉換率**（成約軒數 ÷（試用軒數 ＋ 成約軒數））
5. 最上方顯示**所有業務員的總計**

### 鑽進業務員看細節

頂部會出現分頁列：`[📋 概覽] [王大明] [陳小華] ...`

- 點任一**業務員分頁**或**概覽卡片** → 進入該業務員的客戶列表
- 列表顯示：店名、狀態、聯絡人、地址、試用金額、成約金額、拜訪次數、下次拜訪日
- **搜尋框**：店名／聯絡人／地址即時過濾
- **點任一客戶** → 進入該店家的完整詳情頁：
  - 客戶基本資料、聯絡人、地址、備註、環境觀察
  - 試用品項（含配件、報價）
  - 成約品項（含成約日期）
  - 待辦事項
  - **拜訪紀錄**：每筆 visit 的日期、心情、備註、洞察、狀態變化、下次拜訪日（最新在上）
- 「← 回列表」回到客戶列表
- 「⬇ 匯出 CSV」匯出該業務員所有客戶的試用／成約金額、拜訪次數、待辦數等（UTF-8 BOM 編碼，Excel 直接打開即可）

### 長官如何取得業務員的 URL？

請業務員到自己的 CRM → 設定 → Google Sheets 同步區塊，把上面那條長長的 URL 複製給長官即可。每位業務員的 URL 都不一樣。

### 注意事項

- 此儀表板**唯讀**，不會修改任何業務員的資料（介面上沒有任何寫入按鈕）
- 數字依據業務員當下雲端的資料，建議業務員每天開工前按「從雲端拉最新」確保資料同步
- 若某業務員試算表有未清理的重複店家，儀表板會顯示黃色警告（請業務員到「清理重複店家」處理）

---

## 工業區CRM（陌生開發用）

工業區陌生開發已升級為獨立子系統「**工業區CRM**」——從「查詢」頁右上角的「🏭 工業區CRM」進入。

### 它幫你做什麼

- **內建 5,000+ 家台南工廠名單**（含園區歸屬、行政區、業種、電話、地址），不用再自己準備名錄試算表。
- **開發儀表板**：滲透率、拜訪率、園區×狀態統計，可匯出 CSV 報告。
- **工廠詳情**：窗口、話題、拜訪 check points、客戶反應、標籤、拜訪紀錄、報價試算（用主系統的商品庫）。
- **待訪清單**：指定日期，同區同路自動排一起，方便串路線。
- **回訪管理**：掛「每季回訪」標籤，自動追本季已訪/未訪，可匯出。
- **聚落規則**：非官方工業區（鹽行、大灣一帶等）可照在地慣稱自訂分類。

### 設定雲端同步（每人做一次，約 5 分鐘）

工業區CRM 的雲端**獨立於主系統**，要另外開一份新試算表：

1. 開一份**新的** Google 試算表（建議命名「工業區CRM-你的姓名」）。
2. 擴充功能 → Apps Script，刪掉預設內容。
3. 回到工業區CRM「⚙️ 設定 → 雲端同步」按「📋 複製 Apps Script 程式碼」，貼到編輯器並儲存。
4. 部署 → 新增部署作業 → 網頁應用程式 → 存取權選「**任何人**」→ 部署（步驟與主系統相同，見上方 Step 4 的圖解）。
5. 把 `/exec` 網址貼回「⚙️ 設定 → 雲端同步」，按「測試連線」確認。
6. 有多台裝置（手機/平板/電腦）就每台都貼**同一個**網址，紀錄會自動同步。

> 你的拜訪紀錄只存在你自己的試算表；內建工廠名單是唯讀的、跟著 App 更新。

### 聯繫上了？一鍵建檔到主系統

1. 在工業區CRM 的工廠詳情頁按「**📇 建檔到主系統**」（建議先把窗口填好，會帶入前兩位）。
2. 下次打開主系統時會自動完成建檔（型態＝工廠、狀態＝未拜訪），開發摘要寫進備註。
3. 之後正式經營（拜訪、試用、合約）都在**主系統**做；工業區CRM 的詳情頁會出現
   「主系統客戶（唯讀鏡射）」卡，即時顯示那邊的狀態與最近拜訪——開發紀錄則留在工業區CRM。

### 舊版「工業區名單」使用者請注意

- 原本查詢頁的「🏭 工業區名單」已由工業區CRM 取代。
- 之前用「＋ 建檔」建立的客戶**完全不受影響**，還在客戶列表裡。
- 主系統的雲端**不需要**重新部署。

---

## 工地地圖（施工店面陌生開發）

開車看到**正在施工、裝修中的店面**——那就是還沒開幕的未來客戶。「**工地地圖**」讓你三秒把它登記在地圖上，和同事共享、避免重複拜訪。從「查詢」頁右上角的「🏗 工地地圖」進入。

### 它幫你做什麼

- **站在店門口按「＋ 在這裡登記」**：GPS 自動抓位置（圖釘可拖到正確門口）、拍張照、選施工階段（施工中／裝潢收尾／完工待開幕…），存檔即完成。
- **底圖用內政部電子地圖**：巷弄門牌清楚，可切航照圖看建物外觀。
- **和同事共享**：貼同一個網址，彼此登記的點互相看得到；60 公尺內重複登記會提醒「是不是同一間」。
- **離線也能登記**：收訊差時先存手機，回到有訊號的地方自動補傳。
- **清單模式**：可搜尋、依「離我最近／預計開幕」排序，跑路線時順路看一圈。
- **匯出 CSV**：可直接匯入 Google「我的地圖」給主管看。

### 設定共享同步（全組共用一份，約 5 分鐘）

工地地圖的雲端**獨立於主系統**，而且和工業區CRM 不同——**全組貼同一個網址**（共享才能避免重複拜訪）：

1. 由一個人開一份**新的** Google 試算表（建議命名「工地地圖資料」）。
2. 擴充功能 → Apps Script，刪掉預設內容。
3. 回到工地地圖「⚙ 設定」按「複製」（Apps Script 程式碼），貼到編輯器並儲存。
4. 部署 → 新增部署作業 → 網頁應用程式 → 存取權選「**任何人**」→ 部署（步驟與主系統相同，見上方 Step 4 的圖解）。
5. 把 `/exec` 網址貼回「⚙ 設定」，按「測試連線」確認。
6. **把同一個網址傳給同事**，每個人在設定頁填自己的「登記人姓名」——地圖上就看得出是誰登記的。

> ⚠️ 這個網址等於鑰匙，拿到的人都能看能改。只給同組同事，不要貼在公開群組。

### 快開幕了？一鍵建檔到主系統

1. 在地圖上點那個工地的圖釘，按「**📇 建檔到主系統**」。
2. 下次打開主系統，客戶清單就會多這一家：狀態＝未拜訪、**下次拜訪日＝預計可拜訪日**（時間到自然出現在「今日行程」）、GPS 座標直接沿用（比地址定位準）。
3. 已建檔的點會標「✓ 已建檔」，同事那邊也看得到，不會兩個人重複轉、重複拜訪。

---

## 📣 版本更新紀錄

版本更新紀錄已搬到獨立檔案，方便查閱：

👉 **[CHANGELOG.md](CHANGELOG.md)** — 每一版「你會感覺到什麼」的白話說明

---

## 還有問題？

如果你在設定過程中遇到任何這份指南沒有涵蓋的問題，可以到 GitHub 專案頁面提出 Issue：

👉 https://github.com/coookiepan/salesystem/issues

描述你遇到的問題，附上螢幕截圖的話會更容易幫你排解！

---

> 📝 此指南最後更新：2026 年 6 月
